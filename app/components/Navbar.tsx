// Superseded by components/layout/navbar.tsx (sticky glass nav + animated
// mobile menu). Re-exported here so nothing importing the old path breaks.
export { Navbar as default } from "@/components/layout/navbar";
